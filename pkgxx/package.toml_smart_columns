packager  = "Marc Chantreux <eiro@phear.org>"
url       = "https://github.com/eiro/p6-scripts"
summary   = "like the BSD cols but keep separators alive"
name      = "smart-columns"
version   = "0.0"
build-dependencies = ["ghc"]
dependencies = []
configure = []
sources = [ "https://raw.githubusercontent.com/eiro/p6-scripts/master/haskell/smart-columns.hs" ]
build = [ "ghc smart-columns.hs" ]
install = [ "mkdir -p %{pkg}%{bindir}", "mv smart-columns %{pkg}%{bindir}" ]
